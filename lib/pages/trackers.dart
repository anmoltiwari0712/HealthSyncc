import 'package:flutter/material.dart';

class TrackersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Trackers'),
      ),
      body: Center(
        child: Text('Trackers Page'),
      ),
    );
  }
}

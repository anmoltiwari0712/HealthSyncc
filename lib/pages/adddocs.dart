import 'package:flutter/material.dart';

class AddDocs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Documents'),
      ),
      body: Center(
        child: Text('Add Documents Page'),
      ),
    );
  }
}
